#Creating numpy array and only printing even values 
import numpy as np

# Create a NumPy array containing numbers from 1 to 10
arr = np.arange(1, 11)

# Print only the even numbers
even_numbers = arr[arr % 2 == 0]
print("Even numbers:", even_numbers)
